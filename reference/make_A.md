# Projection matrix for model objects

Generic for computing the projection matrix that links observation
locations to model mesh nodes.

## Usage

``` r
make_A(object, ...)
```

## Arguments

- object:

  A model object.

- ...:

  Additional arguments passed to methods.
