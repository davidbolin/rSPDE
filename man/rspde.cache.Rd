% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_wl2.R
\name{rspde.cache}
\alias{rspde.cache}
\title{Where generated coefficient tables are kept between sessions}
\usage{
rspde.cache(dir, clear = FALSE)
}
\arguments{
\item{dir}{\code{TRUE} to enable the cache at the standard location for this
package, \code{tools::R_user_dir("rSPDE", "cache")}; \code{FALSE} to disable it; a
path to use that directory instead; or missing to query the current setting.}

\item{clear}{Delete the tables that are currently stored?}
}
\value{
The cache directory, or \code{NULL} if the cache is off, invisibly when
called for its effect.
}
\description{
The mesh-free weighted-L2 tables are shipped with the package,
but a table for a particular spectral interval (see \code{\link[=rspde.wl2.table]{rspde.wl2.table()}} and
the \code{kappa_ref} argument of \code{\link[=matern.operators]{matern.operators()}}) has to be computed, which
takes of the order of a second. Within a session such tables are cached in
memory. Enabling this cache stores them on disk as well, so that a second
session, or a script run again, finds them instead of refitting.

The cache is off by default: a package should not write outside the session
temporary directory unless asked to. Calling \code{rspde.cache(TRUE)} is that
request, and creates the directory. The environment variable
\code{RSPDE_CACHE_DIR} sets it for non-interactive use, for instance from
\code{.Renviron}.

Cached files are organised by package version, so upgrading the package does
not read tables produced by an older fit. Within a development version,
clear the cache after changing anything that affects the coefficients.
}
\examples{
rspde.cache()
\dontrun{
rspde.cache(TRUE) # store generated tables between sessions
rspde.cache(clear = TRUE) # throw away what is stored
rspde.cache(FALSE)
}
}
\seealso{
\code{\link[=rspde.wl2.table]{rspde.wl2.table()}}, \code{\link[=matern.operators]{matern.operators()}}
}
