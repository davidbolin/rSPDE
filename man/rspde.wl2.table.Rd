% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_wl2.R
\name{rspde.wl2.table}
\alias{rspde.wl2.table}
\title{Weighted-L2 coefficients for a given spectral interval}
\usage{
rspde.wl2.table(
  m,
  d = NULL,
  nu = NULL,
  alpha = NULL,
  kappa_ref = NULL,
  x_min = NULL,
  C = NULL,
  G = NULL,
  mesh = NULL,
  loc_mesh = NULL,
  type = c("covariance", "operator"),
  eigenvalue = c("bound", "exact"),
  s = 0,
  k_term = FALSE,
  ...
)
}
\arguments{
\item{m}{The order of the rational approximation.}

\item{d}{The dimension of the domain. Taken from \code{mesh} when possible.}

\item{nu, alpha}{The smoothness. Give one of them; \code{alpha = nu + d/2}. Only
the integer part of \code{alpha} is used, since one table covers a whole unit
interval of \code{alpha}.}

\item{kappa_ref}{A lower bound for \code{kappa}, from which \code{x_min} is computed.}

\item{x_min}{The lower end of the spectral interval, if it is known; then
neither \code{kappa_ref} nor the mesh is needed.}

\item{C, G, mesh, loc_mesh}{The finite element matrices or the mesh, used with
\code{kappa_ref} to find \code{x_min}. See \code{\link[=rspde.xmin]{rspde.xmin()}}.}

\item{type}{Either \code{"covariance"} or \code{"operator"}, matching the model the
table is for.}

\item{eigenvalue}{Passed to \code{\link[=rspde.xmin]{rspde.xmin()}}.}

\item{s, k_term}{The weight exponent and the constant term of the fit; see
\code{\link[=rational.coefficients.wl2]{rational.coefficients.wl2()}}. These change what is being approximated and
are for studying the approximation rather than for building a model: a
constant term has no block in the covariance-based construction, and a
table carrying one is refused by \code{\link[=matern.operators]{matern.operators()}}.}

\item{...}{Passed to the fit, for instance \code{n_starts} or \code{by}.}
}
\value{
A table of coefficients, with the attributes \code{type}, \code{d}, \code{m},
\code{m_alpha}, \code{x_min} and \code{kind}, for use as the \code{wl2_table} argument of
\code{\link[=matern.operators]{matern.operators()}}.
}
\description{
Builds a table of weighted-L2 rational coefficients for a
specific lower end of the spectral interval, so that the approximation is
optimal on the interval the discrete operator actually has rather than on
\eqn{(0,1]}.

The mesh-free tables that \code{\link[=matern.operators]{matern.operators()}} uses by default do not depend
on the mesh or on \eqn{\kappa} and are stored in the package, which is why
they cost nothing at set-up. They are, however, fitted on all of
\eqn{(0,1]}, while a discretised operator only ever sees
\eqn{x \in (x_{\min}, 1]} with \eqn{x_{\min}} set by the mesh resolution
and by \eqn{\kappa}. Fitting on that shorter interval spends the same
number of terms where they matter and is appreciably more accurate; the
price is that the table depends on the mesh and therefore has to be
computed, which takes of the order of a second.

With the cache turned on (see \code{\link[=rspde.cache]{rspde.cache()}}) there is usually nothing to
do by hand: a model asked for with \code{kappa_ref} computes its table once and
finds it again in later sessions without being told where it is. The two
routes share one store, so a table built here is what a later
\code{matern.operators(kappa_ref = ...)} picks up, and the other way round.

This function is for what the cache does not cover: holding \eqn{x_{\min}}
fixed across several meshes or models rather than letting each derive its
own, inspecting the coefficients and the errors of the fit, or working
without writing to a cache at all. Pass the result to \code{\link[=matern.operators]{matern.operators()}}
as \code{wl2_table}; supplying it overrides \code{x_min} and \code{kappa_ref}. The result
is an ordinary data frame with attributes, so \code{saveRDS()} is also an option
if you would rather manage it yourself than enable the cache.

Since \eqn{x_{\min}} grows with \eqn{\kappa}, a \emph{lower bound} for
\eqn{\kappa} gives the shortest interval that is certainly long enough,
and hence a table that stays valid while \eqn{\kappa} is estimated. That
is what \code{kappa_ref} is.
}
\examples{
\donttest{
mesh <- fmesher::fm_mesh_1d(seq(0, 1, length.out = 101))
tab <- rspde.wl2.table(m = 2, d = 1, nu = 0.8, kappa_ref = 5, mesh = mesh)
attr(tab, "x_min")
}
}
\seealso{
\code{\link[=rspde.xmin]{rspde.xmin()}}, \code{\link[=matern.operators]{matern.operators()}},
\code{\link[=rational.coefficients.wl2]{rational.coefficients.wl2()}}
}
