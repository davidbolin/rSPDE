% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inla_safe.R
\name{rspde_safe_inla}
\alias{rspde_safe_inla}
\alias{local_rspde_safe_inla}
\title{Load INLA safely for examples and tests}
\usage{
rspde_safe_inla(
  multicore = NULL,
  quietly = FALSE,
  minimum_version = "24.12.01",
  required_symbol = NULL
)

local_rspde_safe_inla(
  multicore = FALSE,
  quietly = TRUE,
  required_symbol = NULL,
  envir = parent.frame()
)
}
\arguments{
\item{multicore}{Logical; if \code{TRUE}, leave INLA's \code{num.threads} option
unchanged. If \code{FALSE}, set it to \code{"1:1:1"}. The default allows multicore
only in interactive sessions outside testthat.}

\item{quietly}{Logical; suppress diagnostic messages when \code{TRUE}.}

\item{minimum_version}{Minimum acceptable INLA version. This should match
the requirement in \code{DESCRIPTION}.}

\item{required_symbol}{Optional name of a required INLA Cgeneric symbol.}

\item{envir}{Environment in which test cleanup handlers are registered.}
}
\value{
\code{TRUE} when INLA is available and safe to use; otherwise \code{FALSE}.

\code{local_rspde_safe_inla()} is called for its testthat skip and local
option-setting side effects.
}
\description{
Checks that a sufficiently recent INLA package and matching executable are
available. In non-interactive use it also limits INLA to one thread for
repeatability. An optional Cgeneric symbol can be required when an rSPDE
feature depends on a model that may not yet be included in every INLA build.
}
\examples{
if (rspde_safe_inla()) {
  # Run INLA-dependent calculations.
}
}
\keyword{internal}
