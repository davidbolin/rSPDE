% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hybrid.spde.R
\name{simulate.hybrid_spde}
\alias{simulate.hybrid_spde}
\title{Simulation of a hybrid Whittle-Matern SPDE model}
\usage{
\method{simulate}{hybrid_spde}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{A \code{hybrid_spde} object returned by \code{\link[=hybrid.spde]{hybrid.spde()}}.}

\item{nsim}{Number of samples to generate.}

\item{seed}{Optional integer used to initialise the random number
generator.}

\item{...}{Additional arguments passed to the underlying simulate
method (e.g. updated parameter values).}
}
\value{
A matrix with \code{nsim} columns and one row per mesh node.
}
\description{
Samples the hybrid SPDE model
\deqn{L^{\alpha/2}(\tau Y(s)) = \beta X(s) + W(s)}
by first sampling the mean-zero part using the simulation method of
the underlying \code{\link[=spde.matern.operators]{spde.matern.operators()}} approximation and then
adding the deterministic mean
\eqn{\mu = \tau^{-1} \beta L^{-\alpha/2} X}.
}
\seealso{
\code{\link[=hybrid.spde]{hybrid.spde()}}
}
