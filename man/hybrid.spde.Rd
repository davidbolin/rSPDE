% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hybrid.spde.R
\name{hybrid.spde}
\alias{hybrid.spde}
\title{Hybrid non-stationary Whittle-Matern SPDE model with non-zero mean}
\usage{
hybrid.spde(
  X = NULL,
  beta_X = NULL,
  kappa = NULL,
  tau = NULL,
  kappa_mu = NULL,
  theta = NULL,
  B.tau = NULL,
  B.kappa = NULL,
  B.sigma = NULL,
  B.range = NULL,
  alpha = NULL,
  nu = NULL,
  parameterization = c("spde", "matern"),
  G = NULL,
  C = NULL,
  d = NULL,
  graph = NULL,
  mesh = NULL,
  range_mesh = NULL,
  loc_mesh = NULL,
  m = 1,
  type = c("covariance", "operator"),
  type_rational_approximation = c("brasil", "chebfun", "chebfunLB"),
  check_stationarity = TRUE
)
}
\arguments{
\item{X}{Numeric vector or matrix containing the covariate field(s)
evaluated at the mesh nodes. If a matrix is supplied, each column is
treated as a separate covariate field. The number of rows must equal
the number of mesh nodes of the underlying FEM discretization.}

\item{beta_X}{Numeric vector of regression coefficients corresponding
to the columns of \code{X}. Defaults to a vector of zeros of length
\code{ncol(X)}.}

\item{kappa, tau, theta, B.tau, B.kappa, B.sigma, B.range, alpha, nu, parameterization, G, C, d, graph, mesh, range_mesh, loc_mesh, m, type, type_rational_approximation, check_stationarity}{Arguments passed to \code{\link[=spde.matern.operators]{spde.matern.operators()}}. The default
\code{check_stationarity = TRUE} allows the underlying covariance model
to be returned as a stationary \code{\link[=matern.operators]{matern.operators()}} object whenever
\code{tau} and \code{kappa} are constant after the log-linear regression on
\code{theta} is applied.}

\item{kappa_mu}{Optional. If \code{NULL} (the default), the operator
\eqn{L_\mu = \kappa^2 - \Delta} used for the deterministic mean
\eqn{\mu = \beta L_\mu^{-\alpha/2} X} shares the range parameter
\code{kappa} with the random component. If a positive scalar (or vector
of length n_mesh for non-stationary models) is supplied,
\eqn{L_\mu = \kappa_\mu^2 - \Delta} is used instead.}
}
\value{
\code{hybrid.spde} returns an object of class \code{"hybrid_spde"}
which also inherits from the class of the object returned by
\code{\link[=spde.matern.operators]{spde.matern.operators()}} (\code{spde_matern_operator} plus either
\code{rSPDEobj} or \code{CBrSPDEobj} depending on \code{type}). The object stores
the covariate matrix \code{X}, the regression coefficients \code{beta_X}, the
cached operator object \code{op_mean} used to evaluate \eqn{L^{-\alpha/2}},
and the evaluated mean function \code{mu} at the mesh nodes.
}
\description{
\code{hybrid.spde} constructs a finite element/rational SPDE approximation
of the model
\deqn{Y(s) = \beta L^{-\alpha/2} X(s) + \tau^{-1} L^{-\alpha/2} W(s),}
where \eqn{L = \kappa(s)^2 - \Delta} is the (possibly non-stationary)
Whittle-Matern differential operator, \eqn{\tau} is a positive scale
that affects only the random component, \eqn{X} is a (matrix of)
covariate field(s) supplied at the mesh nodes, \eqn{\beta} is a
vector of regression coefficients and \eqn{W} is Gaussian white
noise.
}
\examples{
set.seed(1)
x <- seq(from = 0, to = 1, length.out = 51)
kappa <- 10
tau <- 0.5
alpha <- 1.3
X <- cbind(sin(2 * pi * x), cos(2 * pi * x))
beta_X <- c(1.5, -0.5)
op <- hybrid.spde(
  X = X, beta_X = beta_X,
  kappa = kappa, tau = tau, alpha = alpha,
  loc_mesh = x, d = 1, parameterization = "spde"
)
Y <- simulate(op, nsim = 1)
}
\seealso{
\code{\link[=spde.matern.operators]{spde.matern.operators()}}, \code{\link[=simulate.hybrid_spde]{simulate.hybrid_spde()}},
\code{\link[=predict.hybrid_spde]{predict.hybrid_spde()}}
}
