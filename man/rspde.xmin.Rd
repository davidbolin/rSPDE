% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_wl2.R
\name{rspde.xmin}
\alias{rspde.xmin}
\title{Lower end of the spectral interval of a rational approximation}
\usage{
rspde.xmin(
  C = NULL,
  G = NULL,
  mesh = NULL,
  kappa_ref = NULL,
  nu = NULL,
  diameter = NULL,
  loc_mesh = NULL,
  eigenvalue = c("bound", "exact")
)
}
\arguments{
\item{C}{The mass matrix of the finite element discretisation.}

\item{G}{The stiffness matrix of the finite element discretisation.}

\item{mesh}{An optional mesh; \code{C} and \code{G} are computed from it if they are
not given, and it is used for the diameter of the domain.}

\item{kappa_ref}{The reference value \eqn{\kappa_{\mathrm{lo}}}. If \code{NULL},
it is taken to be \code{sqrt(8 * nu) / diameter}.}

\item{nu}{The smoothness parameter, used for the default \code{kappa_ref}.}

\item{diameter}{The diameter of the domain, used for the default
\code{kappa_ref}. Computed from \code{mesh} if not given.}

\item{loc_mesh}{Mesh locations, an alternative to \code{mesh} for the diameter.}

\item{eigenvalue}{Either \code{"bound"} (the default), which uses the Gershgorin
bound \code{max(rowSums(abs(G)) / diag(C))} for \eqn{\mu_{\max}}, or \code{"exact"},
which computes the eigenvalue by Lanczos iteration. The bound is an
over-estimate of \eqn{\mu_{\max}}, and therefore gives a conservative (too
small) \eqn{x_{\min}}, which is the safe direction.}
}
\value{
The value of \eqn{x_{\min}}.
}
\description{
Computes the value \eqn{x_{\min} = 1/(1 + \mu_{\max}/\kappa_{\mathrm{lo}}^2)}
used by the weighted-L2 rational coefficients, where \eqn{\mu_{\max}} is the
largest generalised eigenvalue of \eqn{(G, C)} and
\eqn{\kappa_{\mathrm{lo}}} is a lower bound for the values that \eqn{\kappa}
can take.
}
\details{
The reference \eqn{\kappa_{\mathrm{lo}}} must be a \emph{lower} bound: a reference
above the true \eqn{\kappa} leaves part of the spectrum outside the fitted
interval, and the error then grows by one to two orders of magnitude. A
reference below the true \eqn{\kappa} is safe; the approximation then
degrades gracefully towards the mesh-free fit. The default,
\eqn{\kappa_{\mathrm{lo}} = \sqrt{8\nu}/\mathrm{diam}}, corresponds to a
range equal to the diameter of the domain.
}
\examples{
x <- seq(from = 0, to = 1, length.out = 201)
fem <- rSPDE.fem1d(x)
# reference kappa corresponding to a range equal to the domain diameter
rspde.xmin(C = fem$C, G = fem$G, loc_mesh = x, nu = 0.5)
# a user-supplied lower bound for kappa
rspde.xmin(C = fem$C, G = fem$G, kappa_ref = 10)
}
\seealso{
\code{\link[=rational.coefficients.wl2]{rational.coefficients.wl2()}}
}
