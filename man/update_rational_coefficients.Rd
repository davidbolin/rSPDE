% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_wl2.R
\name{update_rational_coefficients}
\alias{update_rational_coefficients}
\title{Recompute the weighted-L2 coefficients for an estimated kappa}
\usage{
update_rational_coefficients(object, kappa_ref = NULL, safety = 3, ...)
}
\arguments{
\item{object}{A model created by \code{\link[=matern.operators]{matern.operators()}} with
\code{type_rational_approximation = "wl2"}.}

\item{kappa_ref}{The new reference value. If \code{NULL}, the \code{kappa} of the
object divided by \code{safety} is used.}

\item{safety}{The safety factor applied to the \code{kappa} of the object when
\code{kappa_ref} is not given.}

\item{...}{Further arguments passed to \code{update()}.}
}
\value{
The updated model.
}
\description{
Two-stage use of the weighted-L2 rational coefficients. The coefficients
must not depend on \code{kappa} during estimation, since that would make the
likelihood non-smooth in \code{kappa}, so they are computed at set-up for a
conservative reference \code{kappa}. Once \code{kappa} has been estimated, this
function recomputes them once for a reference derived from the estimate, for
the final likelihood evaluation and for prediction.
}
\details{
The reference must remain a lower bound for \code{kappa}, which is why the
default divides the estimate by \code{safety}. A reference above the true \code{kappa}
leaves part of the spectrum outside the fitted interval and the error grows
by one to two orders of magnitude, whereas a reference below it degrades
gracefully towards the mesh-free fit.
}
\examples{
x <- seq(from = 0, to = 1, length.out = 101)
op <- matern.operators(
  loc_mesh = x, nu = 0.4, range = 0.2, sigma = 1, d = 1, m = 2,
  parameterization = "matern", type = "operator",
  type_rational_approximation = "wl2"
)
op <- update_rational_coefficients(op)
}
\seealso{
\code{\link[=rational.coefficients.wl2]{rational.coefficients.wl2()}}, \code{\link[=rspde.xmin]{rspde.xmin()}}
}
