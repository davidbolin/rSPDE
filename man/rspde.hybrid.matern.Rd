% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inla_rspde_hybrid.R
\name{rspde.hybrid.matern}
\alias{rspde.hybrid.matern}
\title{Hybrid Whittle-Matern SPDE model for INLA / inlabru (alpha = 2)}
\usage{
rspde.hybrid.matern(
  mesh,
  X,
  prior.tau = NULL,
  prior.kappa = NULL,
  prior.beta_x = NULL,
  start.ltau = NULL,
  start.lkappa = NULL,
  start.beta_x = NULL,
  ...,
  separate_kappa_mu = FALSE,
  prior.kappa_mu = NULL,
  start.lkappa_mu = NULL,
  debug = FALSE,
  shared_lib = "detect"
)
}
\arguments{
\item{mesh}{An \code{fmesher} mesh (1d or 2d).}

\item{X}{Numeric vector or matrix of covariate fields evaluated at
the mesh nodes; one column per covariate.}

\item{prior.tau}{A list with \code{mean} and \code{prec} (Gaussian prior on
\code{log(tau)}). Defaults: mean = 0, prec = 0.1.}

\item{prior.kappa}{A list with \code{mean} and \code{prec} (Gaussian prior on
\code{log(kappa)}). Defaults: mean derived from the mesh range, prec = 0.1.}

\item{prior.beta_x}{A list with \code{mean} (vector of length \code{p}) and
\code{prec} (vector of length \code{p}). Independent Gaussian priors on the
regression coefficients. Defaults: mean = 0, prec = 0.001
(uninformative).}

\item{start.ltau, start.lkappa}{Starting values for \code{log(tau)} and
\code{log(kappa)}. Default to the prior means.}

\item{start.beta_x}{Numeric vector of starting values for \code{beta_X}.
Default: zeros.}

\item{...}{Additional arguments passed to the underlying model
constructor.}

\item{separate_kappa_mu}{Logical. If \code{TRUE}, the deterministic mean
operator uses its own range parameter \code{kappa_mu}, estimated as an
extra hyperparameter, rather than sharing \code{kappa} with the random
field. Default: \code{FALSE}.}

\item{prior.kappa_mu}{A list with \code{mean} and \code{prec} (Gaussian prior on
\code{log(kappa_mu)}). Only used when \code{separate_kappa_mu = TRUE}. Defaults
to the same prior as \code{prior.kappa}.}

\item{start.lkappa_mu}{Starting value for \code{log(kappa_mu)}. Only used
when \code{separate_kappa_mu = TRUE}. Defaults to the prior mean.}

\item{debug}{Passed to INLA.}

\item{shared_lib}{Which shared lib to use for the cgeneric. See
\code{\link[=rspde.matern]{rspde.matern()}} for details.}
}
\value{
An INLA cgeneric latent model that can be used in INLA
formulae as \verb{f(idx, model = <model>)}.
}
\description{
Creates an INLA cgeneric latent model for the hybrid Whittle-Matern
SPDE
\deqn{Y(s) = \beta_X^T L^{-1} X(s) + \tau^{-1} L^{-1} W(s),}
where \eqn{L = \kappa^2 - \Delta} and the deterministic mean
component \eqn{\beta_X^T L^{-1} X} is part of the latent model
itself: the cgeneric stores the centred field
\eqn{u(s) = \tau^{-1} L^{-1} W(s)} and returns
\eqn{\mu(s) = \beta_X^T L^{-1} X(s)} via the \code{mu} callback. The
coefficients \code{beta_X}, together with \code{tau} and \code{kappa}, are estimated
jointly as cgeneric hyperparameters -- this is the genuine fully
Bayesian fit, not a plug-in approximation.
}
\details{
Currently the implementation is restricted to the case alpha = 2
(which is the main case of practical interest: in 2d this is the
Matern with \code{nu = 1}).

The cgeneric hyperparameters are
\deqn{\theta = (\log\tau,\; \log\kappa,\; \beta_{X,1}, \ldots, \beta_{X,p}).}
The model stores the FEM \code{C} and \code{G} matrices and the covariate
matrix \code{X} and (re)computes
\eqn{Q = \tau^2\, L\, C^{-1}\, L} and
\eqn{\mu = L^{-1}(X \beta_X)} at every INLA call. The mean
computation is a single sparse Cholesky solve done with Eigen.
}
\examples{
\dontshow{if (rspde_safe_inla(required_symbol = "inla_cgeneric_rspde_hybrid_alpha2_model")) withAutoprint(\{ # examplesIf}
library(INLA)
x <- seq(0, 1, length.out = 81)
mesh <- fmesher::fm_mesh_1d(x)
X <- cbind(sin(2 * pi * x), cos(2 * pi * x))
hyb <- rspde.hybrid.matern(mesh = mesh, X = X)
# ... use in formula: y ~ -1 + f(idx, model = hyb)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=hybrid.spde]{hybrid.spde()}}, \code{\link[=rspde.matern]{rspde.matern()}}
}
