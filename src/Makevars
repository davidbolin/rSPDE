# The weighted-L2 fit is part of the normal build, including on CRAN. The
# optional INLA cgeneric sources are copied in by ./configure only when
# compilation is requested, and bring their own Makefile, which supersedes
# this file.
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
